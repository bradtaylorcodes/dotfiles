# Git

## Commit Messages

- Keep commit messages short, ideally under 60 characters.
- Use a single-line commit message unless additional context is necessary.
- Use imperative language, e.g. `Add user validation`, not `Added user validation`.
